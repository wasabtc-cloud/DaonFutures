name: CatchWorld Strategy B Iterative Backtest
on:
  workflow_dispatch:
jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy
      - name: Download Strategy B Scout artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: catchworld-strategy-b-scout-*
          path: strategy_b_input
          merge-multiple: false
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: 35128138456
      - name: Iterative development backtest
        run: python backtest/catchworld_strategy_b_iterative_backtest.py
      - uses: actions/upload-artifact@v4
        with:
          name: catchworld-strategy-b-iterative
          path: results/strategy_b_iterative/
          retention-days: 90
